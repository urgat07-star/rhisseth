DOMAIN=rhisseth.ru
ADMIN_USER=mapadmin
# Generate interactively: docker run --rm -it caddy:2 caddy hash-password
# In Compose .env use single quotes to preserve dollar signs in the hash.
ADMIN_PASSWORD_HASH='REPLACE_WITH_GENERATED_HASH'
